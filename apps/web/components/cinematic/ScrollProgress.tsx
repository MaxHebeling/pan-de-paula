/** Barra de progreso de lectura (2 px). Solo CSS con scroll-timeline; sin soporte no se muestra. */
export function ScrollProgress() {
  return <div className="cin-progress" aria-hidden="true" />;
}
