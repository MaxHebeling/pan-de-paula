# Go-live — acciones externas pendientes

Estado del repositorio al 2026-09-12 (tag `v0.1.0`): código, migraciones, tests, CI, scripts y documentación completos y en verde.
Lo que sigue requiere cuentas, credenciales o decisiones del propietario. Orden recomendado:

**Avance al 2026-09-13 (00:50 UTC):**

- ✅ Supabase: proyectos `pan-de-paula` (`bajcopdiehamtamdydjc`) y `pan-de-paula-staging` (`bkzrychcdcrudwnvcmxu`) en us-west-1, 16 migraciones aplicadas, rol `pdp_app` con contraseña propia, seed aplicado (admin `admin@elpandepaula.mx` con cambio de contraseña forzado), bucket público `product-images` creado. La app usa el pooler en modo transacción (6543) como `pdp_app`; migraciones y respaldos usan el modo sesión (5432). TLS verificado con `certs/supabase-root-2021-ca.pem`.
- ✅ Vercel: `pan-de-paula-web` y `pan-de-paula-admin` con todas las variables (producción y preview), storage en Supabase, auto-deploy por push desactivado.
- ✅ Primer despliegue de producción con `pnpm deploy:prod`: verificación, respaldo `pre-deploy`, migraciones, deploy y smoke OK. Tag `deploy-production-20260913-004534-90d42c6`. URLs: https://pan-de-paula-web.vercel.app · https://pan-de-paula-admin.vercel.app.
- ✅ `packages/db/migrations/RELEASED` congela las 16 migraciones publicadas.
- 🔐 Secretos generados en `.env.production`, `.env.staging` y `.secrets.supabase.local` (locales, fuera de git): moverlos a un gestor de contraseñas.
- ⏳ Pendiente: credenciales de Mercado Pago, Meta/Instagram, Resend y Sentry (pasos 3–6); exportar el Google Sheets (7); datos reales del negocio y fotos (8); cambiar la contraseña del admin al primer login (9); monitor de uptime (10).

| #     | Qué hacer                                                                                                                                                                                       | Dónde                                                                                                           | Por qué                                                                                            | Cómo verificarlo                                                                                                                |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| 1 ✅  | Crear proyectos Supabase `pan-de-paula-staging` y `pan-de-paula` (USD 10/mes cada uno en la organización actual). Crear el rol `pdp_app` con contraseña y usar el pooler en modo _transaction_. | supabase.com → New project; SQL: `alter role pdp_app with password '…'` tras la primera migración               | La app necesita una base gestionada con respaldos; en local no hay Docker para Supabase.           | `DATABASE_URL=… DATABASE_SSL=require pnpm db:migrate` termina con 16 migraciones; `select count(*) from schema_migrations` = 16 |
| 2 ✅  | Crear los dos proyectos en Vercel (`pan-de-paula-web`, `pan-de-paula-admin`) y cargar variables por ambiente.                                                                                   | `bash scripts/vercel-setup.sh staging` y `… production` (necesita `.env.staging` / `.env.production` completos) | Despliegue reproducible con `pnpm deploy:*`.                                                       | `curl https://<admin>/api/ready` responde `ok:true` con `migrations:16`                                                         |
| 3     | Mercado Pago: aplicación, credenciales de prueba y producción, webhook `https://<web>/api/webhooks/mercadopago` (evento `payment`) y su clave secreta; habilitar Point/QR si se usarán.         | developers.mercadopago.com.mx (ver `docs/INTEGRATIONS.md`)                                                      | Cobro online y en terminal. Sin secreto de webhook, producción rechaza notificaciones a propósito. | Pago de prueba de $1 desde `/checkout` → pedido pasa a `paid`; reembolso desde `/pedidos/[id]` refleja `refunded`               |
| 4     | Activar flags en `/configuracion?tab=funciones`: `mercadopago_online`, `mercadopago_point` / `mercadopago_qr`, `email_receipts`, `instagram_bot`.                                               | Admin                                                                                                           | Las funciones de riesgo se despliegan apagadas.                                                    | Cada flag muestra el método en POS/checkout                                                                                     |
| 5     | Meta/Instagram: app, permisos `instagram_manage_messages` + `pages_messaging`, webhook `https://<web>/api/webhooks/instagram`, `META_VERIFY_TOKEN`, App Review.                                 | developers.facebook.com                                                                                         | Bandeja y bot de Instagram.                                                                        | Mensaje real a la cuenta → aparece en `/instagram`                                                                              |
| 6     | Resend con dominio verificado y `EMAIL_FROM`; Sentry DSN para ambas apps.                                                                                                                       | resend.com · sentry.io                                                                                          | Comprobantes y confirmaciones por email; errores con release/ambiente.                             | Email de confirmación llega; Sentry recibe un error de prueba                                                                   |
| 7     | Exportar el Google Sheets (XLSX por pestaña) y correr el importador con `--dry-run` y luego `--apply`.                                                                                          | `docs/MIGRATION_SHEETS.md`                                                                                      | Historial de clientes, precios de insumos, recetas y ventas.                                       | `import_batches` con status `applied`; costos en `/recetas` coinciden con la hoja                                               |
| 8     | Datos del negocio: dirección, teléfono, WhatsApp, Instagram, horarios, ventanas de pedido, puntos de retiro, políticas (transferencia, privacidad), fotos reales de productos.                  | Admin → `/configuracion`, `/productos`                                                                          | El sitio público los muestra tal cual.                                                             | Home y `/horarios` reflejan los datos                                                                                           |
| 9     | Cambiar la contraseña del admin sembrado; crear usuarios por rol.                                                                                                                               | `/usuarios`                                                                                                     | La cuenta seed es conocida.                                                                        | Login con la nueva contraseña; la vieja falla                                                                                   |
| 10    | Primer respaldo y simulacro de restauración en producción; monitor de uptime sobre `/api/ready`; plan Vercel que permita el cron `*/15`.                                                        | `pnpm backup production` · `pnpm restore:drill` · UptimeRobot/BetterStack                                       | Un respaldo no es confiable hasta restaurarlo.                                                     | `backups/restore-drills.log` con RTO                                                                                            |
| 11 ✅ | Al publicar: `bash scripts/release-migrations.sh` y commit de `packages/db/migrations/RELEASED`.                                                                                                | repo                                                                                                            | Congela las migraciones publicadas (CI falla si alguien las edita).                                | `pnpm check:migrations` OK                                                                                                      |
